# Task

Add `.github/dependabot.yml` with `cargo` (directory: `/`) and `github-actions` ecosystems, weekly interval, open-pull-requests-limit capped.

## Why SMALL
Single-file config change; no schema/API/UI boundaries; follows existing project patterns.

## Key files (if the recon found any)
`.github/dependabot.yml` (new file)
