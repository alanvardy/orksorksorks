# Done

- **What was built**: Added `.github/dependabot.yml` with `cargo` and `github-actions` ecosystems, weekly interval, and `open-pull-requests-limit: 10`.
- **Commit SHA(s)**: `8fe38be`
- **Verification**: All 191 tests pass (cargo fmt, check, clippy, nextest, TODO/FIXME/dbg gate).
- **Reviewer findings**: No blockers. Only a stale note in `recon.md` (internal inconsistency about github-actions config) — not a code issue.
- **Remaining manual items**: None.
