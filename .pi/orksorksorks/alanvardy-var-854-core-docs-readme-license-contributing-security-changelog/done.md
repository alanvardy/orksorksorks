# Done

- **What was built**: Core docs for orksorksorks — `README.md`, `LICENSE` (full MIT text), `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md`, `AGENTS.md`, and `docs/adr/` (index README + ADR 0001 "adopt dated ADRs" + ADR 0002 "typed errors" retrospective). Also removed the `DELETEME` placeholder introduced by the ticket's placeholder commit, and squashed the branch to a single clean commit so main's history carries no DELETEME-only commit.
- **Commit SHA(s)**: `6216c0f` (pushed to branch `alanvardy-var-854-core-docs-readme-license-contributing-security-changelog`, PR #23 draft)
- **Verification**: `./scripts/test.sh` green — cargo fmt, cargo check, cargo clippy `--tests -- -D warnings`, 195/195 nextest tests passed, TODO/FIXME/dbg! gate clean. Doc content cross-verified against source (commands in `src/commands/mod.rs`, config schema in `src/config.rs`, resolution in `src/config_dir.rs`, error policy in `src/errors.rs`/`src/format.rs`/`src/main.rs`, CI in `.github/workflows/`, install/test scripts).
- **Reviewer findings**: 1 blocker (P1) fixed — AGENTS.md claimed `cargo llvm-cov` coverage runs on main; it doesn't (no caller passes `upload-coverage`; CI is PR-only). Nits (P2) fixed — README/CHANGELOG overstated config validation (step→script refs are runtime, not parse-checked; corrected to steps/models/prompts), and README/CONTRIBUTING listed only 3 of the gate's 5 forbidden markers (added `DEBUG:`/`FIXTURE:`). Noted, no change: branch-protection facts (CI Gate, 1 review, rebase-only) are repo-settings facts corroborated by the var-850 GitHub-API verification, not verifiable in-tree.
- **Remaining manual items**: PR #23 is still a draft — mark ready for review when desired. Docs state the `linear-project.md` repo stays untouched (it is tracked in main; no change made).