# Done

- **What was built**: `tests/architecture.rs` using rust_arkitect 0.3.7 (dev-dependency) asserting the crate's layering — (1) `commands` imports only its five downward modules (`config`, `config_dir`, `errors`, `format`, `git`) plus the external crates it uses, via an `it_may_depend_on` allowlist; (2) no cycles; (3) no upward imports, via per-module `it_must_not_depend_on` rules covering every upward edge in the total order `main < commands < config < config_dir < git < errors < format`. Wired into the test workflow by auto-discovery only (nextest picks up `tests/*.rs`; no workflow edits needed, Cargo.lock committed for `--locked` CI).
- **Commit SHA(s)**: `4b07e0f546347b0b120774ac5fa7f4b30b164d79` (Add rust_arkitect layer/architecture tests) — includes `tests/architecture.rs`, `Cargo.toml`, `Cargo.lock`, and deletion of the `DELETEME` placeholder.
- **Verification**: `cargo nextest run --test architecture` → 2/2 passed (independently re-run green). Full gate `bash scripts/test.sh` → fmt, check, clippy `--tests -D warnings`, nextest 197/197, forbidden-strings gate all passed. Sad-path mutation check: a new/down-level dependency in `commands` fails the test loudly (e.g. removing the `crate::` allowlist spellings reports `Forbidden dependencies to [crate::config::Model, crate::config::read_config]`).
- **Reviewer findings**: no blockers. Nits: (a) claimed `crate::…` allowlist entries were dead — disproven by the mutation check (they carry the expr/type-path deps); (b) rust_arkitect's engine walks all `.rs` under the repo root and would panic on an unparseable stray `.rs` — inherent to the crate, noted, not fixable in scope; (c) `_reusable-test.yml` lacks `--locked` — pre-existing, out of scope.
- **Remaining manual items**: none. Recon subagent (Pass 1) thrashed and was interrupted; recon was absorbed by the orchestrator directly (import graph, conventions, and the rust_arkitect 0.3.7 API verified from crate source), so the deliverable is unaffected. If desired, a follow-up could guard against the whole tree being scanned (e.g. confirm no scratch `.rs` files land in the repo root).